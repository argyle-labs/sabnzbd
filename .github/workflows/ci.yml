name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: argyle-labs/.github/.github/workflows/plugin-ci.yml@main
    secrets: inherit
